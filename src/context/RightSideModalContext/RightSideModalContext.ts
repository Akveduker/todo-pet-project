import { createContext } from "react";
import { RightSideModalContextType } from "types/context/rightSideModalContext/rightSideModalContext";

export const RightSideModalContext = createContext<RightSideModalContextType>(null)